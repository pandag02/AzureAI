<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Azure AI Chat</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Azure AI와 대화</h1>
    
    <!-- 질문 입력 폼 -->
    <form action="/query" method="POST">
        <input type="text" name="text" placeholder="질문을 입력하세요" required>
        <button type="submit">전송</button>
    </form>

    <!-- FastAPI 응답 출력 -->
    <h2>응답:</h2>
    <p><%= responseText || "응답이 없습니다." %></p>

    <!-- 대화 히스토리 출력 -->
    <% if (history && history.length > 0) { %>
        <h2>이전 대화 기록</h2>
        <ul>
            <% history.forEach(msg => { %>  <!-- 최신 대화가 아래쪽에 오도록 변경 -->
                <% if (msg.role === "user") { %>
                    <li><strong>사용자:</strong> <%= msg.content %></li>
                <% } else if (msg.role === "assistant") { %>
                    <li><strong>AI:</strong> <%= msg.content %></li>
                <% } %>
            <% }) %>
        </ul>
    <% } %>

</body>
</html>
